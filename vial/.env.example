# Environment variables for Vial MCP Controller
# Copy this file to .env and update values as needed
# Rebuild: If missing, recreate with these defaults and update secrets

NODE_ENV=development
PORT=8080
OAUTH_CLIENT_ID=vial_mcp_client
OAUTH_CLIENT_SECRET=your_client_secret
OAUTH_REDIRECT_URI=http://localhost:8080/auth/callback
DB_PATH=./vial.db
LOG_LEVEL=info

# Rebuild Instructions: Place in /vial/. If missing, recreate with the above content. Update OAUTH_CLIENT_SECRET as needed. Used by /vial/src/server.js. Run Troubleshoot to check for errors.

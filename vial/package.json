{
  "name": "vial-mcp",
  "version": "1.0.0",
  "scripts": {
    "start": "node src/server.js",
    "build": "npx esbuild static/bundle.js --bundle --minify --outfile=static/assets/bundle.min.js --tree-shaking=true",
    "test": "jest"
  },
  "dependencies": {
    "express": "^4.18.2",
    "sqlite3": "^5.1.7",
    "ajv": "^8.12.0",
    "redaxios": "^0.5.1",
    "lz-string": "^1.5.0",
    "mustache": "^4.2.0",
    "dexie": "^4.0.7"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "jest": "^29.5.0",
    "supertest": "^6.3.3"
  }
}

# Instructions:
# - Install: `npm install`
# - Build: `npm run build`
# - Run: `npm start`
# - Test: `npm test`

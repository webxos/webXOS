# Dockerfile for Vial MCP Controller
# Builds Node.js server environment
# Rebuild: Run `docker build -t vial-mcp-controller .`

FROM node:18

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 8080

CMD ["node", "src/server.js"]

# Rebuild Instructions: Place in /vial/. Ensure package.json includes dependencies: express, sqlite3, ws, jsonwebtoken, ajv, lz-string. Run `docker build -t vial-mcp-controller .`. Check /vial/errorlog.md for issues.

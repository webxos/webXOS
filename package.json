{
  "name": "webxos",
  "version": "1.0.0",
  "description": "Server MCP Controller for WebXOS with neural network and PWA support",
  "main": "server/server.html",
  "scripts": {
    "start": "netlify dev",
    "build": "echo 'No build step required for static site and functions'",
    "deploy": "netlify deploy --prod",
    "test": "echo 'No tests specified' && exit 0"
  },
  "dependencies": {
    "@netlify/functions": "^2.8.1"
  },
  "devDependencies": {
    "netlify-cli": "^17.34.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/webxos/webxos.git"
  },
  "author": "WebXOS Team",
  "license": "MIT",
  "keywords": [
    "webxos",
    "netlify",
    "serverless",
    "pwa",
    "neural-network"
  ]
}

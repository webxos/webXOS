{
  "name": "webxos",
  "version": "1.0.0",
  "description": "WebXOS MCP Server Controller",
  "main": "server/server.html",
  "scripts": {
    "start": "netlify dev",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {},
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
